import React from 'react'
import { Drawer, Box, Typography, Divider, Stack } from '@mui/material'
export default function RecipeDrawer({ recipe, onClose }){ const open=Boolean(recipe); const r=recipe||{}; return (<Drawer anchor="right" open={open} onClose={onClose} PaperProps={{ sx: { width: { xs: '100%', sm: 420 } } }}><Box className="p-4 space-y-3"><Typography variant="h6">{r.title}</Typography><Typography variant="subtitle2" color="text.secondary">{r.cuisine||'—'}</Typography><Divider /><Stack spacing={1}><Stack direction="row" justifyContent="space-between"><Typography className="text-gray-600">Total Time</Typography><Typography>{(r.total_time??'-') + (r.total_time?' mins':'')}</Typography></Stack><Stack direction="row" justifyContent="space-between"><Typography className="text-gray-600">Serves</Typography><Typography>{r.serves??'-'}</Typography></Stack></Stack><Divider /><Typography variant="subtitle1" className="font-semibold">Description</Typography><Typography className="text-sm text-gray-700 whitespace-pre-wrap">{r.description||'No description available'}</Typography></Box></Drawer>) }
